{
  "servers": {
    "assistant-flow": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "${MCP_ASSISTANT_DIR}",
        "mcp-assistant"
      ],
      "env": {
        "ASSISTANT_FLOW_ROOT": "${ASSISTANT_FLOW_ROOT}"
      }
    }
  }
}
