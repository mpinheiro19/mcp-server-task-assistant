# Path to the mcp-assistant project root (auto-detected from git root by setup.sh)
MCP_ASSISTANT_DIR=/absolute/path/to/mcp-assistant

# Root directory that contains the copilot-assistants/ folder
ASSISTANT_FLOW_ROOT=/absolute/path/to/Codes
